import { Direction } from '../enums/direction.enum';

export const keyToDirectionMap = new Map([
    ['ArrowUp', Direction.UP],
    ['ArrowDown', Direction.DOWN],
    ['ArrowLeft', Direction.LEFT],
    ['ArrowRight', Direction.RIGHT],
]);
